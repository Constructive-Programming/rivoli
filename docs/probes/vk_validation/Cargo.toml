[package]
name = "vk_validation_probe"
version = "0.0.0"
edition = "2024"
description = "Does the Vulkan validation layer actually fire on this machine? See ../README.md"
publish = false

[dependencies]
ash = "0.38"

# Its own workspace root: this probe must never join rivoli's build, lint surface, or
# dependency graph. It is an instrument you run by hand, not part of the crate.
[workspace]
