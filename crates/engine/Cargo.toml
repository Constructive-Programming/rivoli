[package]
name = "rivoli-engine"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
# Forwarded down the DAG: `--features rocm` at any level arms the one backend.
rocm = ["rivoli-backend/rocm"]

[lints]
workspace = true

[dependencies]
rivoli-core = { path = "../core" }
rivoli-artifact = { path = "../artifact" }
rivoli-oracles = { path = "../oracles" }
rivoli-backend = { path = "../backend" }
