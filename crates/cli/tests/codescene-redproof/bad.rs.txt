//! Deliberately unhealthy: deep nesting, long function, many arguments, duplication.
pub fn process(a: i32, b: i32, c: i32, d: i32, e: i32, f: i32) -> i32 {
    let mut total = 0;
    if a > 0 {
        if b > 0 {
            if c > 0 {
                if d > 0 {
                    if e > 0 {
                        if f > 0 {
                            total += a + b + c + d + e + f;
                        } else {
                            total -= a + b + c + d + e;
                        }
                    } else if f > 1 {
                        total += a * 2;
                    } else {
                        total -= 1;
                    }
                } else if e > 1 {
                    total += b * 2;
                } else {
                    total -= 2;
                }
            } else if d > 1 {
                total += c * 2;
            } else {
                total -= 3;
            }
        } else if c > 1 {
            total += d * 2;
        } else {
            total -= 4;
        }
    }
    if a > 10 {
        if b > 10 {
            if c > 10 {
                if d > 10 {
                    total += a + b + c + d;
                } else {
                    total -= a + b + c;
                }
            } else {
                total -= 5;
            }
        } else {
            total -= 6;
        }
    }
    total
}
