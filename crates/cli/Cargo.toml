[package]
name = "rivoli"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
# Forwarded down the DAG: `--features rocm` at any level arms the one backend.
rocm = ["rivoli-engine/rocm"]

[lints]
workspace = true

[dependencies]
rivoli-engine = { path = "../engine" }
rivoli-core = { path = "../core" }
anyhow = "1"

[dev-dependencies]
serde_json = "1"
